# 実機検証（2026-09-08）

Chrome に `dist/chrome-mv3/` を読み込み、ライブ配信とアーカイブで以下を確認。すべて問題なし（ユーザー報告）。

| 項目 | 根拠 GDR | 結果 |
|---|---|---|
| スキップ行（±0.1 秒〜5 分）で再生位置が動く、一時停止中も可 | DOM-002 | OK |
| 「ライブより N 秒前」（`seekable.end − currentTime`）がライブ端で安定し、戻した分だけ増える | DOM-002 | OK（追従を採用のまま） |
| 記録がスキップ後の再生位置に追従する | DOM-002 | OK |
| 📷 / Alt+Shift+S で `~/Downloads/ss/` に PNG 保存、同じ瞬間の記録（note = ファイル名）、バッジ `SS` | EXT-002 / DOM-002 | OK |
| 📁 で最新スクショを Finder 表示 | EXT-002 | OK |
| アーカイブで「● 記録」が有効になり再生位置が記録される（`source: "position"`、補正は無効） | DOM-003 | OK |

## GDR 再検討条件への影響

- DOM-002「`seekable.end(0)` がライブ端を正しく示さない配信形態」→ 通常のライブ配信では発生せず。プレミア公開は未検証
- 未記録: `offsetSec` の再調整幅、data URL のサイズ・所要時間、📁 のフォールバック頻度（改善提案 §5-6）
- GDR-UI-005（空セクション撤回）は本検証の後に実装。全削除後の見え方は未検証
