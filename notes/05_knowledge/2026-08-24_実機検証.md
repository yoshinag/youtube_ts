# 実機検証（2026-08-24）

Chrome に `dist/chrome-mv3/` を読み込み、YouTube ライブ配信で以下を確認。すべて問題なし（ユーザー報告）。

| 項目 | 根拠 GDR | 結果 |
|---|---|---|
| Alt+Shift+T で記録、バッジに件数 | EXT-001 | OK |
| popup「● 記録」ボタンで記録、非ライブページで無効化 | UI-003 | OK |
| `startTimestamp` が `ytInitialPlayerResponse` から取得できる | DOM-001 | OK（ライブ配信で確認） |
| 配信タイトル・チャンネルの取得、配信別アコーディオン表示 | STORE-002 / UI-004 | OK |
| note のインライン編集、補正値の保存と反映 | UI-002 | OK |
| テキストコピー / JSON 書き出し、ダーク表示 | STORE-001 / UI-001 | OK |

## GDR 再検討条件への影響

- DOM-001「`startTimestamp` が取得できない配信形態が頻出」→ 通常のライブ配信では発生せず。プレミア公開・限定公開は未検証
- 未検証: v1 データからの migration（検証環境に v1 データが無かった可能性）、5000 件上限、複数タブ同時記録
