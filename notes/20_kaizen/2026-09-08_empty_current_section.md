# 改善提案: 記録が 0 件の「現在の配信」空セクションを出さない

> 雛形: `notes/91_gdr/_reference/templates/T2_BUILDUP_RECORD.md`
> 作成: 2026-09-08

**GDR 一覧:**

| GDR ID | 決定の要約 | status |
|---|---|---|
| GDR-UI-005 | popup の一覧は記録がある配信だけを出す。現在の配信に記録が無くても空セクションは作らない（GDR-UI-004 の「空セクションを先頭に出す」を撤回） | Accepted |

---

## 1. GDR（General Decision Record）

**GDR-UI-005: 記録が 0 件の配信はセクションを出さない**

- **status:** Accepted
- **scope:** ui
- **決定:**
  - `groupRecordsByStream` は記録のある配信だけをグループにする。現在の配信に記録が無くても空グループを先頭に挿入しない
  - 現在の配信に記録があるときの扱い（先頭固定・展開・「現在」バッジ・`info` のタイトルで仮メタを補完）は変えない
  - グループが 0 件なら `#empty` の案内文だけを表示する（文言は「動画ページで ● 記録 / Alt+Shift+T」に合わせて更新）
  - `view.ts` の空グループ用 `li.empty-row` と `EMPTY_MESSAGE` は不要になるので削除する
- **理由:**
  - 「全削除」の直後に現在の配信のセクションが「0 件」で残り、削除されていないように見える（ユーザー報告 2026-09-08）。GDR-UI-004 で空セクションを出した根拠「記録直後に同じ場所へ行が増える」は、記録すればどのみち先頭に現れるので空セクションが無くても成り立つ
  - 記録できるかどうかは「● 記録」ボタンの有効 / 無効で伝わっており、空セクションが担う情報は無い
  - **代替案 A: 全削除の直後だけ空セクションを隠す** → 状態が増え、popup を開き直すとまた出る。却下
  - **代替案 B: 現在の配信名をツールバーに常時表示し、セクションは出さない** → 情報としては悪くないが、幅の狭い popup で 1 行増える。記録が入れば見出しに出るので今は不要。却下（再検討条件）
- **影響:**
  - `src/lib/streams.ts` の `groupRecordsByStream` から空グループ挿入を削除。`current` 引数は先頭固定とタイトル補完のために残す
  - `streams.test.ts` の「現在の配信に記録が無ければ空グループを先頭に置く」系のテストを反転
  - `entrypoints/popup/view.ts` の `EMPTY_MESSAGE` / `empty-row` を削除、`index.html` の `#empty` 文言を更新
- **再検討条件:**
  - 「今どの配信を開いているか」を popup で確認したい要望が出た → 代替案 B（ツールバーに現在の配信名）
- **日時:** 2026-09-08T00:00:00+09:00
- **関連:** refines GDR-UI-004（空セクションの撤回）; relates-to GDR-UI-003

---

## 2. 現状

1. 「全削除」を押しても、開いているタブの配信が「0 件」の空セクションとして残り、削除が効いていないように見える
2. 空セクションは GDR-UI-004 の仕様だが、記録可能かは「● 記録」ボタンで分かるため、表示上の役割が重複している

---

## 3. 改善案

記録のある配信だけをセクションにし、0 件なら案内文だけにする。現在の配信の先頭固定・展開・タイトル補完はそのまま。

---

## 4. 詳細セクション

### 4.1. `groupRecordsByStream`

```ts
if (current) {
  const idx = groups.findIndex((g) => g.current);
  if (idx > 0) { /* 先頭へ移動（変更なし） */ }
  // idx < 0 のときの空グループ挿入を削除
  if (idx >= 0 && groups[0].placeholder && current.title) { /* タイトル補完（変更なし） */ }
}
```

### 4.2. popup

- `#empty`: 「まだ記録がありません。YouTube の動画ページで「● 記録」または Alt+Shift+T を押してください。」
- `view.ts` から `EMPTY_MESSAGE` と `empty-row` 分岐を削除

---

## 5. 検討課題

1. **`toExportText` への影響** — `current: null` で呼んでいるので空グループは元から出ない。変更なし
2. **記録直後の見え方** — 0 件 → 1 件目の記録で先頭にセクションが現れる。空セクションが行に変わる現在の挙動と体感差は小さい

---

## 6. 実行計画

### 6.1. タスク一覧

| # | タスク | 根拠 GDR | 依存 | ステータス |
|---|---|---|---|---|
| 1.1 | `groupRecordsByStream` の空グループ挿入を削除、テスト反転 | UI-005 | — | 未着手 |
| 1.2 | `view.ts` / `index.html` の空行・文言整理、typecheck / test / build | UI-005 | 1.1 | 未着手 |
| 2.1 | 実機検証（全削除後にセクションが残らない、記録すると先頭に現れる） | UI-005 | 1.2 | 未着手 |

### 6.2. 進捗サマリー

| フェーズ | タスク数 | 完了 | 残 | コミット |
|---|---|---|---|---|
| 1 | 2 | 0 | 2 | — |
| 2 | 1 | 0 | 1 | — |

---

## 7. ふりかえり

（実装後に記入）
